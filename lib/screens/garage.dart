import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';

class Garage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          Text('hola mundo', style: TextStyle(fontSize: 50))
        ],
      )
    );
  }
}
